{
  "name": "make-jp",
  "version": "1.0.0",
  "description": "for Maker Faire Tokyo",
  "main": "common.js",
  "repository": "https://github.com/sforzando/make-jp.git",
  "bugs": {
    "url": "https://github.com/sforzando/make-jp/issues"
  },
  "author": "sforzando LLC.",
  "license": "WTFPL",
  "private": true,
  "dependencies": {
    "jquery-validation": "^1.17.0",
    "materialize-css": "^0.100.2",
    "toastr": "^2.1.4"
  },
  "devDependencies": {
    "eslint": "^4.18.2",
    "node-sass": "^4.7.2",
    "parcel-bundler": "^1.6.2",
    "prettier": "^1.11.1",
    "prettier-eslint": "^8.8.1",
    "prettier-eslint-cli": "^4.7.1"
  },
  "scripts": {
    "start": "dev_appserver.py app.yaml",
    "build": "yarn run build:debug",
    "build:debug":
      "yarn run build:debug:common && yarn run build:debug:registration && yarn run build:debug:survey",
    "build:debug:common":
      "./rewrite_variables.sh && parcel build src/common.js --no-minify --target browser --detailed-report --out-dir static",
    "build:debug:registration":
      "parcel build src/registration.js --no-minify --target browser --detailed-report --out-dir static",
    "build:debug:survey":
      "parcel build src/survey.js --no-minify --target browser --detailed-report --out-dir static",
    "build:production":
      "yarn run build:production:common && yarn run build:production:registration && yarn run build:production:survey",
    "build:production:common":
      "./rewrite_variables.sh && parcel build src/common.js --no-cache --target browser --detailed-report --out-dir static",
    "build:production:registration":
      "parcel build src/registration.js --no-cache --target browser --detailed-report --out-dir static",
    "build:production:survey":
      "parcel build src/survey.js --no-cache --target browser --detailed-report --out-dir static",
    "cover": "openssl enc -e -aes256 -in app.yaml -out app.yaml.secret",
    "uncover": "openssl enc -d -aes256 -in app.yaml.secret -out app.yaml"
  }
}
